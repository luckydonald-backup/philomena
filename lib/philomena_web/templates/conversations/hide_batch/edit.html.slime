h2 Edit Hide batch

= render "form.html", Map.put(assigns, :action, Routes.conversations_hide_batch_path(@conn, :update, @hide_batch))

= link "Back", to: Routes.conversations_hide_batch_path(@conn, :index)
